API_ID = 000
API_HASH = ''
BOT_TOKEN = ""
DATABASE_URL = "sqlite+aiosqlite:///ww_database.db"
CLIENT_ID = 000
# This will prevent the account from getting banned by telegram.
# But this will slow down the process.
# Recommended...
SAFE_MODE = {
    "active": True, # Just switch this to True | False
    "send_banner_every": 1, # Do not change this, modify it from the manager pannel.
    "sleep": 10,
    "auto_join_every": 30
}